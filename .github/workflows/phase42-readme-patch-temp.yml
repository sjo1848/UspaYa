name: Phase 4.2 README Patch Retired

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporary README patch workflow retired; safe to delete."
