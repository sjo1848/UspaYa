# Docker

La fundación utiliza `compose.yaml` en la raíz para PostgreSQL local.

Las imágenes de API, web y worker se incorporarán cuando exista una primera vertical desplegable. No se agregan contenedores vacíos solo para completar la estructura.
